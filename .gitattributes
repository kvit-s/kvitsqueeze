# Normalise line endings so a Windows checkout and a Linux one agree.
* text=auto eol=lf

# Windows-only helpers must keep CRLF or cmd.exe mis-parses them.
*.bat text eol=crlf

# Binary assets, never diffed or line-ending-converted.
*.png binary
*.jpg binary
*.ico binary
*.exe binary
